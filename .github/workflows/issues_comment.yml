name: Create a comment on new Issues

on:
  issues:
    types: [opened]  

jobs:
  comment-with-action:
     runs-on: windows  
     steps:
       - name: "dump github context"
         run: echo '${{ toJSON(github.event) }} | jq
         shell: bash
         
